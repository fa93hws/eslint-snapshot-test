name: CI

on:
  push:
    paths-ignore:
      - '**.md'

jobs:
  install-deps:
    name: Install node modules
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Find Yarn Cache
        uses: actions/cache@v1
        with:
          path: node_modules
          key: yarn-${{ hashFiles('yarn.lock') }}
          restore-keys: yarn-
      - name: Run yarn
        run: yarn
      - name: Find linting & testing Cache
        uses: actions/cache@v1
        with:
          path: cache
          key: eslintcache-${{ hashFiles('yarn.lock')}}
          restore-keys: eslintcache-
      - name: Check ESLint
        run: yarn lint --cache --cache-location "cache/eslint.json"
      - name: Check Typescript Types
        run: yarn lint:ts --incremental --tsBuildInfoFile "cache/ts.jsn"
  # types:
  #   name: Check Typescript types
  #   needs: install-deps
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v1
  #     - name: Check Typescript types
  #       run: yarn lint:types
  # format:
  #   name: Check code format
  #   needs: install-deps
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v1
  #     - name: Run prettier
  #       run: yarn prettier
  # unit-tests:
  #   name: Run unit tests
  #   needs: install-deps
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v1
  #     - name: Run unit tests
  #       run: yarn test
